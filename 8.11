def en(n, k=1, total=0):
    total += 1 / k
    if total > n:
        return total
    else:
        return en(n, k + 1, total)
n = float(input("число n: "))
result = en(n)
print(f"первое число в последовательности большее {n} это {result}")
